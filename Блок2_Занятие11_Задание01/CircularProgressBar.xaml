<UserControl x:Class="Блок2_Занятие11_Задание01.CircularProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Блок2_Занятие11_Задание01"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">

    <Viewbox>
        <Grid Height="100" Width="100">
            <Ellipse 
                Stroke="LightGray" StrokeThickness="5" RenderTransformOrigin="0.5,0.5"
                Width="100" Height="100" 
                HorizontalAlignment= "Center" VerticalAlignment="Center" />

            <Path x:Name="pathElli" Stroke="Blue" StrokeThickness="3" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                  RenderTransformOrigin="0.5,0.5"
                  >
                <Path.RenderTransform>
                    <RotateTransform 
                                Angle="{Binding ElementName=RotateAngleLineRotate, Path=Angle}"/>
                </Path.RenderTransform>
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="50,0">
                            <ArcSegment Point="{Binding endPointPresent, RelativeSource={RelativeSource AncestorType=local:CircularProgressBar}}"
                                        Size="50,50" SweepDirection="Clockwise" IsLargeArc="{Binding IsLargeArcNeeded, 
                                RelativeSource={RelativeSource AncestorType=local:CircularProgressBar}}" />
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>
            
            <TextBlock Text="{Binding TextOutValue,RelativeSource={RelativeSource AncestorType=local:CircularProgressBar}}" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"  FontWeight="Bold" />

        </Grid>
    </Viewbox>
</UserControl>
