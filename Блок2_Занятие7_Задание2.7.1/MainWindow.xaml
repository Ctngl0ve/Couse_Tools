<Window x:Class="Блок2_Занятие7_Задание2._7._1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Блок2_Занятие7_Задание2._7._1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border>
                                <Grid>
                                    <Rectangle x:Name="rectangle_1" 
                                       Height="10" Width="10" RadiusX="3" RadiusY="3" Margin="0,0,0,15"                                           
                                               VerticalAlignment="Center" HorizontalAlignment="Center"                                                
                                               Stroke="#FFD96FC7" 
                                               
                                               />
                                    <ContentPresenter Content="{TemplateBinding Content}" 
                                              Margin="0,15,0,0"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              OpacityMask="Black" 
                                                      />
                                    <TextBlock x:Name="checked" 
                                               Text="x" FontWeight="Bold"
                                               Margin="0,0,0,18.8"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" 
                                               Foreground="#FFD96FC7"/>
                                    <Ellipse x:Name="indeterminate" 
                                              Margin="0,0,0,15"  Width="4" Height="4"
                                             HorizontalAlignment="Center" VerticalAlignment="Center" 
                                             Fill="#FFD96FC7"
                                            />

                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checked" Property="Opacity" Value="1"/>
                                <Setter TargetName="indeterminate" Property="Opacity" Value="0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="checked" Property="Opacity" Value="0"/>
                                <Setter TargetName="indeterminate" Property="Opacity" Value="0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="indeterminate" Property="Opacity" Value="1"/>
                                <Setter TargetName="checked" Property="Opacity" Value="0"/>
                            </Trigger>
                            
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" Content="Checked" IsChecked="True" />
        <CheckBox Grid.Column="1" Content="Unchecked" IsChecked="False"/>
        <CheckBox Grid.Column="2" Content="Indeterminate" IsChecked="{x:Null}"/>
    </Grid>
</Window>
