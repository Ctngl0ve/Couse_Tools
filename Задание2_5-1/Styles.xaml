<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">



    <SolidColorBrush x:Key="PrimaryColor" Color="#FF758EF5"/>
    <SolidColorBrush x:Key="HoverColor"   Color="#FFBAC5F1"/>
    <SolidColorBrush x:Key="PressedColor"  Color="#FFB1F1AC" />
    <SolidColorBrush x:Key="DisabledColor" Color="#FFA0ADB3" />

    <Style TargetType="Button" x:Key="BaseButtonStyle">
        <!--текст кнопки-->
        <Setter Property="FontFamily"  Value="Futura PT Light "/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="White"/>
        <!--кнопка-->
        <Setter Property="Width" Value="250"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="BorderBrush" Value="Blue"/>
        <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
        <Style.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource PressedColor}"/>
            </Trigger>
        </Style.Triggers>

    </Style>
    <Style TargetType ="TextBlock" x:Key="HeaderTextStyleTextBlock">
        <Setter Property="FontSize" Value="25"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontFamily"  Value="Futura PT Heavy"/>
        <Setter Property="Margin" Value="0,20,0,10"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="FontFamily"  Value="Futura PT Light "/>
    </Style>

</ResourceDictionary>